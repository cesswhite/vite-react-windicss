const colors = require('windicss/colors')

module.exports = {
    darkMode: 'class',
    plugins: [],
    theme: {
      extend: {
        colors: {
          teal: colors.teal,
        },
      }
    },
  }